<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data noupdate="1">

        <!-- Category -->
        <record id="module_category_player" model="ir.module.category">
            <field name="name">Player</field>
            <field name="sequence">1</field>
        </record>

        <!-- Group: User -->
        <record id="group_player_user" model="res.groups">
            <field name="name">User</field>
            <field name="category_id" ref="module_category_player"/>
            <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
        </record>

        <!-- Group: Manager -->
        <record id="group_player_manager" model="res.groups">
            <field name="name">Manager</field>
            <field name="category_id" ref="module_category_player"/>
            <field name="implied_ids" eval="[(4, ref('group_player_user'))]"/>
        </record>

        <!-- Record Rule: only apply when height != 2 -->
        <record id="player_comp_rule" model="ir.rule">
            <field name="name">Player</field>
            <field name="model_id" ref="model_player"/>
            <field name="domain_force">[('height', '!=', 10)]</field> <!--phan nay la dieu kien de co the thuc hien duoc cac quyen ben duoi (player nao co chieu cao khac 2 thi moi sua, xoa,... duoc -->
            <field name="perm_read" eval="False"/>
            <field name="perm_write" eval="False"/>
            <field name="perm_create" eval="False"/>
            <field name="perm_unlink" eval="True"/>
        </record>

    </data>
</odoo>
